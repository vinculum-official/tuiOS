# tuiOS
A linux distro based on Fedora Linux (https://fedoraproject.org/) and focused mainly on terminals. Uses i3 for WM; only pre-installed gui app is a web browser. You are advised to only installed terminal apps using tpr, the terminal package repository. tpr also contains some browsers, incase you wish to have a new one.
